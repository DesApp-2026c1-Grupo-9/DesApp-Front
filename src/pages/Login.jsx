import React from 'react';
import { Typography } from '@mui/material';

const Login = () => {
  return (
    <Typography variant="h4">
      Iniciar Sesión
    </Typography>
  );
};

export default Login;
